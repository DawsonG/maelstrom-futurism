import React, { ReactNode } from "react";

const Step = ({
  children
}: //isActive,
//transitions
{
  children: ReactNode;
  //isActive: boolean,
  // transitions?: string,
}) => <div>{children}</div>;

export default Step;
