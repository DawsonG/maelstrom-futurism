import { describe, expect, test } from 'vitest';

describe('MfColor', () => {
    test('it creates an object from color name', () => {

    });

    test('it creates an object from a hex string', () => {

    });

    test('it creates an object from rgb or rgba string', () => {

    });
});