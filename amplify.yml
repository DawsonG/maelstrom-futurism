version: 1
applications:
  - appRoot: website
    frontend:
      phases:
        preBuild:
          commands:
            - cd .. && npm ci
        build:
          commands:
            - cd .. && npm run build --workspace=packages/core
            - cd .. && npm run build --workspace=packages/button --workspace=packages/form --workspace=packages/icons --workspace=packages/layout --workspace=packages/modal --workspace=packages/navbar --workspace=packages/paper --workspace=packages/sidebar --workspace=packages/table --workspace=packages/tooltip --if-present
            - cd .. && npm run build --workspace=packages/mf-bot-differentiator --if-present
            - cd .. && npm run build --workspace=website
      artifacts:
        baseDirectory: dist
        files:
          - '**/*'
      cache:
        paths:
          - ../node_modules/**/*
